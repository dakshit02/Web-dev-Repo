// ques 4

let p = prompt("Enter PRINCIPLE");
let r = prompt("Enter RATE");
let t = prompt("Enter Time");

parseInt(p);
parseInt(r);
parseInt(t);

let SI = (p * r * t) / 100
console.log("The SI IS :-", SI)
